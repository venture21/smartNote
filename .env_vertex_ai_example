# ============================================
# Vertex AI 설정 추가
# .env 파일 끝에 아래 내용을 추가하세요
# ============================================

# GCP 프로젝트 ID (필수)
# GCP Console (https://console.cloud.google.com)에서 확인
VERTEX_AI_PROJECT_ID=your-gcp-project-id

# Vertex AI 리전 (선택사항, 기본값: us-central1)
VERTEX_AI_LOCATION=us-central1

# ============================================
# 인증 설정 (둘 중 하나 선택)
# ============================================

# 방법 1: gcloud CLI 사용 (개발용 권장)
# 터미널에서 실행: gcloud auth application-default login

# 방법 2: 서비스 계정 키 사용 (프로덕션 권장)
# GOOGLE_APPLICATION_CREDENTIALS=C:/path/to/service-account-key.json
